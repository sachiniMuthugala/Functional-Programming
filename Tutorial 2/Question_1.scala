package com.smut.scala

object Question_1 {
  def main(args: Array[String]) {
    var i, j, k :Int = 2;
    var m, n :Int = 5;
    var f :Float = 12.0f;
    var g :Float = 4.0f;
    var c :Char= 'X';

    println("k + 12 * m = " + (k + 12 * m));
    println("m / j = " + (m / j));
    println("n % j = " + (n % j));
    println("m / j * j = " + (m / j * j));
    println("f + 10*5 +g = " + (f + 10*5 +g));
    i = i + 1;
    println("++i * n = " + (i * n));
  }
}
